# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
~~~
i=int(input())
j=int(input())
z=int(input())

f = lambda a, b,c: a+b+c

print(f(i, j,z))
~~~

## Output
<img width="535" height="415" alt="442896655-df270927-b951-4377-9362-5ca11d0c2b69" src="https://github.com/user-attachments/assets/b48240de-4b10-4733-930f-490d74b6a2da" />

## Result
The program successfully defines a lambda function that computes the sum of two numbers and prints the result.

